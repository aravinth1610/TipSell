spring:
  application:
    name: TipSell-GateWay
                          
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/

management:
  tracing:
    sampling:
      probability: 1.0
                          
              